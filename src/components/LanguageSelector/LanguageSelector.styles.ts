import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    padding: 0,
  },
  title: {
    marginBottom: 8,
  },
  segmented: {
    marginTop: 8,
    minWidth: 190,
    alignSelf: 'center',
  },
})
