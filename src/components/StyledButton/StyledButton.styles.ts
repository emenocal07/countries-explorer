import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  wrapper: {
    position: 'relative',
    alignItems: 'center',
  },
  measureText: {
    position: 'absolute',
    opacity: 0,
    left: 0,
    top: 0,
    fontSize: 16,
    paddingHorizontal: 16,
  },
})
